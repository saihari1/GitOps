apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: skillzverse

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: kops-csi-1-21

  resources:
    requests:
      storage: 10Gi
